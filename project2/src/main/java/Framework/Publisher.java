package Framework;

public class Publisher implements Runnable{

    @Override
    public void run() {

    }

    // multi instances

    //read in the JSON dataset, parse to each individual objects
    // publish each item
}
